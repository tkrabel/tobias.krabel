# Pip Cheatsheet

``` bash
pip list | grep "bamboolib"  # Version of one package
pip show bamboolib  # same as above, but with more details (author, license, requires)

pip install --upgrade bamboolib
pip install bamboolib==1.11.0
```